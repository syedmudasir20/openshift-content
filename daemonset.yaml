apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: agent
spec:
  selector:
    matchLabels:
      agent: web
  template:
    metadata:
      labels:
        agent: web
    spec:
      containers:
      - name: web
        image: nginx
