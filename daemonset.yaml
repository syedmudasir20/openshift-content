apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: webserver
spec:
  selector:
    matchLabels:
      agent: webserver
  template:
    metadata:
      labels:
        agent: webserver
    spec:
      containers:
      - name: webserver
        image: nginx
